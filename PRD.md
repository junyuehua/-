# PRD — 清明上河图互动Web App

版本：v1（首次完整整理） | 关联文档：`CLAUDE.md`（现状速览）、`decisions-log.md`（决策推理过程）

---

## 1. 产品概述

一个让观者与高清《清明上河图》（故宫博物院藏，北宋张择端本）互动的网页应用。观者可以放大画作欣赏细节，并与预先标注好的画面元素互动，学习画中描绘的北宋人文历史信息。

产品哲学：**画面为主，UI服务于画面。** 所有壳层控件的设计都要服务于"看画"这个核心体验，不能喧宾夺主。

原作实物尺寸（故宫博物院官方数据）：绢本，淡设色，纵24.8厘米，横528厘米。

---

## 2. 核心目标（两大主功能）

1. **放大画作**，仔细欣赏笔触与细节
2. **与预标注的画面元素互动**（分类：建筑 / 人物 / 物件 / 植物 / 动物），了解对应的历史信息

---

## 3. MVP 范围

### 3.1 核心浏览体验
- 主画布支持拖拽平移、滚轮缩放
- 缩放范围【2026-07-10 改版，废弃旧"锁定最小100%（扫描分辨率）"规则】：最小 = **整卷完整入画**（fit 全览，随视口尺寸动态计算），画外露出的部分显示莲花纹渐变背景；最大暂定为扫描分辨率的500%，等瓦片架构最高精度层明确后再定
- **比例基准 = 实物比例**：100% = 画在屏幕上呈现真实物理大小（按画心高24.8cm与CSS 96dpi折算，≈扫描分辨率的12.3%）——产品目的是让用户直观感受这幅画本来的大小，从而体会细节刻画之精妙。物理精度受限于浏览器拿不到真实屏幕DPI，按CSS标准折算即可
- 初始视图 = 卷首（内容最右端）+ 实物比例100%，如同站在真迹前展开画卷
- 不做阻塞式整图加载，采用深度缩放瓦片技术（类似OpenSeadragon/IIIF）：先加载低分辨率全览图，高清瓦片按视口和缩放级别按需流式加载；加载进度条含义 = "当前视口清晰度"，不是"整图加载进度"

### 3.2 观看模式（二选一，可切换）
- **沉浸观画模式**：UI默认全部淡出，无标注/无壳层干扰，纯欣赏
- **学习模式**：UI默认常驻可见，画面元素可交互（标注可见+可点击）
- 默认打开为学习模式

### 3.3 卷轴呈现方式（MVP仅一种）
- MVP只做**完全展开**：连续展开的长卷视图，标注/hotspot互动在此模式下生效
- **分段臂展**（还原手卷阅读体验的替代格式）连同它的切换控件已**移出MVP**，见§7路线图；MVP阶段不存在格式切换，也就不需要对应的checkbox或`S`快捷键

### 3.4 导航与定位
- 位置指示组件：卷轴缩略图导航条，**通栏铺满整个屏幕宽度**（不做居中限宽）
- 可点击导航条任意位置跳转到对应卷轴位置

### 3.5 缩放控制
- 缩放百分比实时读数（比例toast，基准=实物比例，见§3.1）：缩放时出现，停留约1秒后自动消失（时长待实测调整），不常驻
- 一个快捷缩放按钮：**恢复实际大小**（跳转到实物比例100%），**不常驻**——只随比例toast一起出现/消失，且仅当当前比例≠100%时显示；行为上只改变缩放倍数，**保持当前观看位置的中心点不变**，不跳转到卷首等固定位置
- **"放大到最大"按钮已移出MVP**，见§7路线图——原因是它依赖深度缩放瓦片架构的最高精度层设计，该架构本身尚未详细设计，先把MVP范围收窄到"恢复实际大小"这一个明确的按钮

**单击放大：**
- 在画布空白处单击（非拖拽）= 在当前比例上放大50%（×1.5，2026-07-10 从×1.3上调），缩放中心精确锚定在点击的像素位置（复用滚轮缩放已验证的"锚定鼠标位置"变换数学），有200-300ms左右的缓动过渡动画，不是瞬间跳变
- **不做双击放大**——只有单击这一档，因此单击可以立即触发缩放，不需要延迟等待判断"是否会有第二次点击"
- 单击与拖拽平移的判定阈值建议比常规UI更宽松一点（约5-8px的移动容差），因为"误判成单击"的代价是一次意外缩放，比"误判成拖拽"的代价更高，需要更保守地偏向判定为拖拽
- **所有UI元素（marker、按钮、信息卡、导航条等）自身的点击都必须stopPropagation**，不能让点击事件冒泡到画布层触发这个单击放大——画布层的单击放大只对真正点在空白内容上的点击生效
- **marker点击 = 触发同样的放大动作，但缩放中心是marker的精确锚点坐标，不是原始鼠标点击位置**——比一般空白点击更精确，效果是"点一个知识点=精确凑近它"。这个动作叠加在悬停展示信息卡之上，不是替代关系（见3.8悬停交互）

### 3.6 UI 壳层

**顶部（元控件区）：**
- 沉浸/学习模式切换（视觉形式待定：toggle开关 or tab并列按钮）
- 双语切换toggle（中文 / English，全局切换，不做并排显示，默认中文）
- 背景音乐开关（默认关闭）

**底部：**
- 通栏缩略图导航条（见3.4）
- 缩放按钮（恢复实际大小）+ 缩放百分比读数（见3.5），视觉上作为一个"缩放控制簇"整体呈现，与导航条区分开

**壳层可见性规则（顶部/底部共享同一套机制）：**
- 学习模式：默认常驻显示，不自动淡出
- 沉浸模式：默认全部淡出；鼠标移动进入该UI所在的边缘区域（如顶部/底部一定高度的热区）才淡入显示；鼠标离开该区域后（可加小幅缓冲防抖动）立即淡出——**触发条件是"鼠标进入UI所在区域"，不是"画面上任意鼠标活动"**，因为本产品沉浸模式下的核心操作（拖拽平移、滚轮缩放）本身需要持续的鼠标移动，如果用"任意移动都唤醒UI"的常见视频播放器逻辑，UI会在用户探索画面时持续闪现，起不到沉浸效果
- **热区"武装"机制**（2026-07-10 定案）：刚切入沉浸模式时热区不立即生效——因为切换动作发生在顶部热区内，若立即响应，UI 退场后又马上被唤回，体验像 bug。必须先观察到鼠标离开过热区一次，该热区才开始响应进入事件
- **沉浸模式下比例 toast 照常出现**（2026-07-10 定案）：toast 是瞬时反馈不算常驻 UI，缩放时正常弹出并自动消失，不受"UI 全部淡出"规则约束

### 3.7 键盘快捷键
- `I` — 切换沉浸/学习模式
- `L` — 切换语言
- `M` — 音乐开关
- `?` — 弹出全部快捷键速查浮层
- 所有快捷键在UI淡出、不可见状态下依然生效，不依赖鼠标先唤醒壳层——键盘路径应完全独立于鼠标可见性状态
- 顶部各按钮hover时展示tooltip，显示对应快捷键

### 3.8 标注 / 交互机制（核心功能，详细规格）

**视觉语言：朱笔圈点（单色朱红手绘圈），悬停时marker消失、原地被信息卡取代（不做常驻/悬停引线）**
- 每个可交互点位用一个**朱红色的手绘圈（圈点）**表示，带毛笔笔触质感 + Multiply 混合，读作"钤/画在绢上"而不是浮在屏幕上（放弃了早期的纯色矢量圆点方案，理由见 §6 与 `decisions-log.md`）
- **静止态一律单色朱红，不再用分类色区分**：静止态只负责传达"这里有东西可交互"，类别身份（建/人/物/木/兽）延后到悬停卡片里的字印上揭示——这条推翻了早期"marker 自带分类色"的设定，是消掉"彩虹散点"的关键
- **marker 按缩放门槛分层显隐，不再任何缩放都常驻**（详见下方"分层显隐"；这也把 §6 曾否决的 min_zoom 方案以"绑层级"的形式有条件重新引入）
- marker 尺寸：实物 100% 以内**固定屏幕像素**；超过后按 **√实物比例** 温和放大（封顶 3×，热区/聚合/计数字同步）。【2026-07-11 修订：原"任意缩放都固定屏幕像素"在 300%+ 实测圈异常小又细；√ 增速慢于画面，保留"放大后存在感递减"的方向、但不再看不清，仍不需要动态透明度淡化逻辑】
- 静止态 marker **无 idle 动画**（2026-07-11 取消呼吸动效——手绘笔触的天然不规则已提供足够的"非死板"感，动效反而让满屏圈点显得躁动）；"不死板"改由三种手绘圈随机分配实现（见下一条）
- **三种手绘圈变体随机分配**（按点位 id 稳定哈希，刷新不变）：让画面中的圈笔触各异，不是每个都长一样

**分类（可多选，共5类）：** 分类只是数据/筛选维度，**不再决定静止态 marker 的颜色**（静止态统一朱红）。下面这5个分类色现在只用在**悬停信息卡的字印**上（见"悬停交互"），实际 hex 见 §10（Figma 是 source of truth）：
| 分类 | 颜色 | Hex |
|---|---|---|
| 建筑 | 胭虹（暗红） | `#7C4449` |
| 人物 | 青蓝 | `#32788A` |
| 物件 | 灰蓝/石板 | `#6B798E` |
| 植物 | 灰绿 | `#6A8D52` |
| 动物 | 赭石/琥珀 | `#BE8A2F` |

一个点位可同时属于多个分类（tags数组），筛选逻辑 = 命中任一激活标签就显示。多标签用于"同一坐标点内容上确实横跨多个分类"的场景，不要用来表示"两个东西离得近但坐标不同"（那种情况应该分开建点，见下方"建筑与部件的标注惯例"）。

**层级（tier，管 marker 大小 / 内容深度 / 是否参与聚合 / 显隐门槛）：**
| 层级 | 定义 | 圈的大小 | 显隐门槛 | 聚合 |
|---|---|---|---|---|
| 地标层 | 全卷导航锚点级别的地标（如虹桥、孙羊正店、城门楼） | 大圈 | 缩小/总览时仍显示（当导航锚点），内容可写得更丰富 | 不参与 |
| 场景层 | 能用"动词+主语"描述的活动/情节（如说书人讲古、税吏查货） | 中圈 | 中等缩放开始浮现 | 参与 |
| 细节层 | 只能用"名词"描述的单一静态物件/人物（招牌文字、器物） | 小圈 | 到实物100%附近才出现 | 参与 |

**形态统一为"圈"，大小＝层级：** 三个层级都用手绘朱圈，只靠大小区分——地标=大圈、场景=中圈、细节=小圈（放弃了"细节用点、地标用印"的分形态想法）。大小是三档离散基准值（28/18/14 屏幕像素，实物 100% 以上整体乘 markerGrowth 温和放大；细节档 2026-07-11 由 12 调到 14 并加粗描边 0.7→0.9，实测 12 偏小），档间要留足间距、并压住手绘圈的天然大小抖动，避免"偏大的细节圈"和"偏小的场景圈"撞脸；如果三档实测分不清，在"仍是圈"的前提下用笔画粗细/双圈做次级区分，不退回点/印。

**圈落在目标上或旁边，不框住目标**（框住太复杂、画面元素边缘不好框）：偏移用固定屏幕像素、且优先落向目标靠空绢的一侧，配合悬停卡片标题，保证不含糊地指向被标注元素。

**分层显隐（缩放门槛，绑层级）：** 标识只在画面放大到**实物100%（即默认初始视图的尺度）及更近**时才显示，缩小到总览时隐藏——缩得太小时元素本就看不清，显示圈是多余信息，圈是配合"看清元素"才有意义。因为初始视图就是实物100%，实际效果＝"总览干净、进入阅读态就有圈"。门槛**也绑层级**：地标层缩小/总览时仍可见、继续当全卷导航锚点；场景层中等缩放开始浮现；细节层到实物100%附近才出现。过渡用一小段缩放区间淡入/晕开（约80%→100%），不在正好100%那一帧硬弹出。总览既要干净又要能寻路，就把"哪里有内容"的提示交给底部缩略图导航条去背，主画布保持纯净。（此条把 §6 曾否决的"一刀切 min_zoom"以绑层级、地标豁免的形式重新引入，绕开了当初的可发现性顾虑。）

**tier与分类（tags）是两个独立维度，互不绑定：** 聚合豁免是由tier（地标层）决定的，不是由分类（建筑）决定的——理论上一个建筑分类的点位不一定是地标层，一个地标层的点位也不一定是建筑分类。

**内容标注惯例（authoring convention，非schema硬约束）：** 这幅画里值得单独标"建筑"分类的点位实际上都够得上地标级别，因此约定——建筑分类的点位默认按地标层处理。建筑的具体部件（门、窗、招牌）不归进建筑分类，改标物件类，且**各自独立建点**，不要用多标签压在建筑本体那一个坐标点上——因为部件和建筑本体本来就是画面上两个不同的锚点坐标，分开建点也让两者各自拥有独立的标题/正文。这条约定不在schema或标注工具里做硬编码强制（不因为选了"建筑"就锁死tier），保留手动例外的灵活性。

**精度实现：**
- marker锚点坐标存储为真实完整卷轴分辨率（160348×7435px，已切金色画+上下裁边后的基准）下的content-space像素坐标，与工具/应用内部实际渲染用哪份分辨率的图片无关
- 渲染时用跟画布图块相同的pan/zoom变换矩阵计算marker锚点的屏幕位置——因为是单点坐标不是形状轮廓，不存在近似误差，任何缩放倍数下都精确贴合，不会漂移
- marker相对锚点的偏移量（不落在内容正中心，避免遮挡被标注元素本身）用固定屏幕像素定义，不随缩放放大

**悬停交互：**
- 悬停时marker本身**淡出/消失**，信息卡在其附近原地浮现——**不画连回锚点的引线**
- 卡片摆放硬规则：**绝不能遮住该点所标注的画面元素本身**（锚点周围留一圈"保护区"，具体判定算法/半径留到后续讨论，可能随tier变化）；**可以**遮住其他不相关的邻近marker/内容——这两条约束替代了引线，解决"密集区卡片对应哪个点"的辨认问题
- **marker视觉消失不等于交互移除**：点击热区、"点击=精确放大到锚点"这个行为在marker被卡片取代期间依然生效
- 信息卡内容 = **标题 + 正文**；正文用 Markdown 式约定承载引用块——**以 `>` 开头的行渲染成金色引用 block**（空行分段，可多段、可与正文穿插），其余是普通正文段；解析实现见 `annotator-tool.html` 的 `parseBlocks`（app 卡片端复用同一套，与行内链接共用 markdown 解析）。不做分类/层级 tag pill 展示，不做"查看更多"按钮。**正文/引用块内可有行内链接**，语法 `[显示文字](目标)`：目标为 `https://…` = 外链（新标签打开）、`#<点位id>` = 站内跳到另一个标注点（相机运镜过去并展开）；链接颜色统一用 UI红（§10），不随分类变化。**内容约定：金色引用块宜放与正文不重复的内容**（原始文献、题记、点睛引语等），别把正文原句再抄一遍——两段几乎一样会显得冗余
- **印章(seal)视觉**：信息卡上用一个汉字印章图标表示分类，颜色用该分类的分类色（§10那5个色）——**分类色如今只在这里用，静止态 marker 已经不带分类色了**。目前只确认"建"对应建筑；人物/物件/植物/动物对应的字尚未最终拍板，等用户设计图定稿后更新
- **信息卡需要支持交互**：内容长时可内部滚动查看，内容里的链接可点击跳转。为此marker和卡片要共享同一个悬停状态（两者各自监听mouseenter/mouseleave，任一方在悬停中就视为整体在悬停），并在鼠标离开时加一个约250-300ms的缓冲延迟再判定隐藏——如果这段时间内鼠标成功移入了卡片，就取消隐藏。这样鼠标从marker移动到卡片之间的空隙不会导致卡片提前消失。如果这个缓冲延迟的方案在实测中不够用（比如卡片被自动避让弹得较远），可以升级成"安全三角形"（marker-鼠标-卡片三点围出的三角区域内移动都不判定为离开）这种更宽容的几何判定方式，作为后续优化，不是MVP必须
- **卡片本身的wheel事件必须stopPropagation**：卡片内部滚动查看长内容时，不能让这个滚动同时触发画布层的滚轮缩放
- **卡片本身的click事件必须stopPropagation**：点击卡片内的链接或任意内容，不能触发画布层的单击放大（见3.5）

**密集区域聚合（clustering）：**
- 判断标准是**当前屏幕像素距离**，不是缩放百分比——两点屏幕距离低于阈值就合并成聚合标记，超过阈值就拆成独立marker，这条规则天然适应不同区域的内容密度差异，不需要按区域手动调参
- 地标层不参与聚合，只有场景层和细节层互相之间参与聚合运算
- 双阈值防抖动（大致40-60px区间，具体数字**留到真实内容实测再定，不在MVP设计阶段拍死**）。附带一提：上面的"分层显隐"本身已经把密度稀释了大半，聚合逻辑大概率能做得更轻
- **聚合标记 = 一个"这里还有东西、放大看"的入口，不再展示悬停列表。** 理由：列表给的是抽象标题，而这套内容的意义全在画面里——看到真实场景之前，标题（如"税吏查货"）没有意义；列表把人从"看画"拽进"读目录"，还和"点击放大"功能重复
- 悬停聚合标记**只给一个轻提示**：光标变放大镜 + 首次进入的引导，尽量不常驻文字（常驻说明文字压在画上本身违和，且用户学会一次即冗余）
- **只有一条点击路径**：点击聚合标记 = 以它为中心相机运镜放大到超过裂散阈值，聚合裂成独立的圈；滚轮手动放大到该区域同样会裂开。聚合不是特殊模式，只是重叠的圈被临时折叠的形态，用任何方式凑近都会展开
- **裂开即一次性全放出、允许轻微重叠，不做多级次聚合**——用户已主动凑近，就不再设第二道关卡
- 有意识接受的代价：去掉列表也就失去"在密集区一步直达某个具体点"的能力。可接受——这是探索式浏览不是搜索，且抽象标题对这套内容没价值；定向跳转留给路线图里的主题筛选/逐项浏览
- **聚合标记样式**：**实心朱圈**（对比单点圈的空心，"填满＝内容多"，这个视觉逻辑自解释），中间用**汉字数字**显示聚合了几个（一…九），**超过九显示"众"**（"众"读作"这里很多"，不用数字也懂，留在画的字语言里）。聚合标记保持**单一固定尺寸**（不参与"大小＝层级"，数量交给中间的字），且不应比地标大圈更抢眼。颜色用朱红即可——既然静止态已全部单色，"聚合用中性色以免混进分类色"的旧理由已失效；质感上同样做"进绢"（像一枚朱印/一团朱砂晕、数字用留白负形透出），不要平涂徽章感

### 3.9 语言与音频
- 双语：中文 + 英文，全局toggle切换，默认中文，不做中英并排常显
- 背景音乐：可开关，默认关闭

### 3.10 平台与范围边界
- 仅支持网页（不适配手机，检测到移动端时提示"请使用更大屏幕观看"；平板暂不考虑，与手机同样提示或直接不支持，本期不投入适配工作）
- 不需要登录，不做任何跨会话的观看历史记录（当前会话内的临时状态，如卷轴呈现方式的选择，属于正常应用状态，不算"历史记录"，刷新页面后重置为默认值）

### 3.11 分段主题导览（章节导航）

全卷按叙事切成 **5 个连续段落**，每段一个主题名。它是介于"底部空间导航条"（连续/像素级）和"点级 marker"（知识点）之间的**第三层语义导航**——有名字的叙事章节。注意它与路线图里的"主题线索跨卷筛选"不同：那是给点位打标签、跨全卷横向筛选；这是连续的空间分段。

**五段（叙事顺序卷首→卷尾；拼接图里 x 从右 160348 到左 0，故段1在最右）：**

| # | 主题（中 / 英） | content-x 区间 | focal_x | focal_w |
|---|---|---|---|---|
| 1 | 春郊乡野 / Spring Countryside | 120800–160348 | 126551 | 11061 |
| 2 | 汴河码头 / The Bian River Docks | 89170–120800 | 107398 | 12553 |
| 3 | 飞虹卧波 / The Rainbow Bridge | 62031–89170 | 82895 | 11604 |
| 4 | 关厢街市 / Outside the Gate | 36274–62031 | 55469 | 11818 |
| 5 | 东京梦华 / Dreams of the Eastern Capital | 0–36274 | 28223 | 15961 |

（命名推敲见 `decisions-log.md`：如"关厢"= 城门外依墙而生的商业区，与"城内"呼应；"东京梦华"化用《东京梦华录》；宋代已无坊市制，故弃"坊市"。）

**顶部章节栏（当前段指示 + 跳转，二合一）：**【2026-07-11 首版已落地，形态由"单枚题签+点击展开"改为下面这种】
- 落地形态：顶栏正中一排**常驻平铺的 5 个纸签按钮**（每段一个）——一眼看全部章节、不用点开就能发现。比原设计"单枚题签点开才见全部"更好发现，采纳此改动。视觉按 Figma `分段导航`(108:3782) 定稿：纸面80%（`rgba(251,246,234,0.8)`）+ 金字（`#7F6019`，衬线 Bold 14px）、**无描边**、非现代 pill；单签高 38 / padding 8×16 / 签间距 4，圆角只在整条两端 2px。顶栏左右已被模式/语言/音乐占用，正中留给它不冲突。【2026-07-11 二稿：早先"金描边+UI红激活态"描述作废，改为下述透明度方案】
- **只在学习模式显示**；沉浸模式随壳层一起淡出。
- **必须高亮当前段**：视口中心 x 落在哪段，哪段按钮就是激活态，随平移**实时切换**（越界轻 crossfade、不硬跳）。激活态 = 透明度提满（激活 opacity 1、非激活 0.6，hover 同激活），不用 UI红。这个组件既是导航、也是"我现在在哪一章"的定位器，不能只能跳转却不指示当前。
- **任何缩放都显示**（放大、失去全局时最需要它当方位锚点；同地标层逻辑）。
- **排列方向 = 镜像画面空间**：从左到右 = 东京梦华…春郊（段5→段1），和拼接图左右一致、也顺手卷"右起左收"的传统。刻意保留（不改成叙事 1→5 正序）；可加章节序号做方向暗示。

**交互与数据源：**
- 点某段 = 相机运镜到**该段开头**：视口右缘对齐段右界 `x_end`（手卷右起左收，段首就是右边界）。落点保证读者从段首开始向左看**不漏任何画面**。【2026-07-11 改：原"运镜到 focal_x 策展镜头"作废——策展落点可能跳过段首到落点之间的画面】
- **所有显示标题必须读 `segments.json`（唯一数据源），不许在 UI 里另存一份硬编码**——否则改名两头对不齐。（已发生的 bug：UI 硬编码把"春郊乡野"误写成"春娇乡野"；正字是"春郊"，且要改为从数据读取。）
- 可选统一：段边界也可标到底部导航条上，让顶部语义章节与底部连续空间对齐（是否做待定）。

**focal（跳转镜头宽度；`focal_x` 已不参与跳转）：**
- `focal_w` = 视口横向要框住多少 content px，决定落点缩放；`focal_x` 字段保留在 schema/数据里但当前跳转不使用（原"策展定场镜头"语义，2026-07-11 改为段首落点后废弃，留作将来"段内看点"素材）。
- 目标缩放**现算、不存固定倍数**：`scale = 视口宽度(屏幕px) / focal_w`，横向让视口右缘对齐 `x_end`（即镜头中心 = `x_end − focal_w/2`）、纵向居中，结果受 app 缩放上下限钳制。存"框多宽"而非"放大几倍"→ 换屏 / 换设备构图不变（大屏渲染更大但框住的内容一致）。
- `focal_w` 为空则兜底 = 框满整段（宽度用整段宽 `x_end−x_start`，此时落点即整段居中）。

**数据：** `app/segments.json`（schema 见 §4）。采集用 `prototypes/segment-tool.html`（分段工具：点击定分界、"设为当前视图"抓 focal、实时题签预览、导出/导入 JSON）。

---

## 4. 数据模型

标注点位的标准schema（已在标注工具中验证）：

```json
{
  "id": "唯一标识",
  "x": "真实完整卷轴分辨率(160348×7435，已切金色画+上下裁边)下的content-space像素坐标 x",
  "y": "同上，y",
  "tier": "地标 | 场景 | 细节",
  "tags": ["建筑", "人物", "物件", "植物", "动物"],
  "title_zh": "标题（中）",
  "title_en": "标题（英）",
  "body_zh": "正文（中）。以 > 开头的行 = 金色引用块，空行分段（Markdown blockquote 约定，可多段、可与正文穿插）",
  "body_en": "正文（英），同上约定"
}
```

坐标以真实完整分辨率为唯一基准，与实际渲染用的工作副本分辨率解耦——工作副本分辨率以后升级不需要重新换算已标注的数据。

**分段主题 schema（§3.11，`app/segments.json`）：**

```json
{
  "id": "1..5",
  "title_zh": "主题（中）",
  "title_en": "主题（英）",
  "x_start": "该段 content-x 左界（含），段5固定 0",
  "x_end": "该段 content-x 右界（不含），段1固定 160348",
  "focal_x": "跳转落点：镜头横向对准的 content x（可空）",
  "focal_w": "跳转落点：视口横向框住的 content 宽度，决定缩放（可空，空则框满整段）"
}
```

5 段连续且完整覆盖 `[0, 160348]`；**叙事顺序（卷首→卷尾）在 x 上是从右往左**（段1 春郊在最右、段5 东京梦华在最左）。与标注点位共用同一套 160348×7435 坐标基准。

---

## 5. 技术架构要点

- **加载策略**：深度缩放瓦片技术，避免整图阻塞加载，见§3.1
- **坐标系统**：真实完整卷轴分辨率（**160348×7435px**）为官方坐标系（**source of truth，已锁定**）。这版由故宫版原始扫描件`Full scroll/1-4.jpg`按右→左顺序拼接、切掉卷首金色装裱画、再上下裁掉扫描白/黑背景边（顶100/底60全分辨率px）后得到；官方master文件为项目根目录的`清明上河图-完整拼接.jpg`。应用内部无论用什么分辨率的素材渲染，标注坐标始终以此为准。旧的182054基准（含金色画）已作废——见§8
- **marker渲染**：锚点用content-space坐标+画布同一套pan/zoom变换矩阵；marker偏移用screen-space固定像素，尺寸=固定基准×markerGrowth（实物100%以内=1）——两套坐标空间的换算已在原型中验证成立

---

## 6. 已评估并放弃的方案

以下方案讨论后明确放弃，实现时不应重新引入，详细推理见`decisions-log.md`：

1. **轮廓贴合"背光"效果**（hover时给画面元素本身描边发光）——手描轮廓在不同缩放倍数下无法保持精确贴合，且684个点位的手描成本过高
2. **用min_zoom一刀切gate所有点位的"存在"**（不分层级，缩放不到阈值就全部不显示）——这个一刀切版本仍然放弃，因为会让用户彻底错过知识点，违背"学习模式要让人发现历史信息"的初衷。**但注意：一个"绑层级的分层显隐"已被采纳**（地标层缩小时仍显示当导航锚点，场景/细节层才随放大逐层浮现，见§3.8"分层显隐"）——它保留了"细节缩小时是噪音"的好处，又用地标豁免解决了可发现性，跟被否的"一刀切"是两回事
3. **缩小到某个百分比区间才显示indicator（一刀切版本）**——同上，一刀切版本放弃；现改为§3.8的"绑层级分层显隐"
4. **高倍缩放时marker动态透明度淡化+悬停唤醒**——决定不做这套动态逻辑，改用marker固定屏幕尺寸+低调静态视觉设计来覆盖同样的问题，保持逻辑简单
4b. **5个分类色直接给静止态marker上色**（早期设定）——放弃：五色高饱和圆点铺在退色绢本上＝彩虹散点，杂乱且与画面材质格格不入。改为静止态统一单色朱红的"朱笔圈点"，分类色只在悬停卡片的字印上出现（见§3.8）
4c. **用纯色矢量圆点当 marker**——放弃：干净的矢量圆读作"贴在屏幕上的现代UI"。改为带笔触质感 + Multiply 混合的手绘朱圈，读作"钤/画在绢上"
5. **悬停信息卡固定dock展示**——决定改用floating，理由见§3.8
6. **悬停时常驻画一条连回锚点的引线**——早期定案，后来被用户自己的proof of concept推翻：改为悬停时marker本身消失、卡片原地取代，靠"卡片不能遮住自己标注的锚点、但可以遮住其他不相关内容"这条硬规则解决辨认问题，不再需要线。详见`decisions-log.md`
7. **建筑分类天然不参与聚合**——不准确的表述，已澄清聚合豁免是tier（地标层）决定的，不是分类（建筑）决定的；"建筑默认按地标层处理"只是一条内容标注惯例，不是架构规则

---

## 7. MVP 之外（路线图，明确不在本期范围）

- **分段臂展格式 + 切换checkbox**——还原手卷阅读体验的替代呈现方式（一次一臂展宽度，不带标注互动），连同它"仅在沉浸模式下出现"的contextual checkbox和`S`快捷键，一并移出MVP。原MVP设计已包含完整规格（见decisions-log.md），以后要做时可以直接复用
- **"放大到最大"按钮**——跳转到最高可用精度倍数的快捷缩放按钮，移出MVP，等深度缩放瓦片架构的最高精度层设计明确后再考虑加回来
- **方案B：next/prev逐项浏览组件**——把标注内容封装成独立UI，点击时相机自动运镜对齐每个点位，信息卡固定dock展示，列表内容随当前视野动态变化。作为学习模式主交互方式（floating悬停）之外的**补充**功能，不替代主交互模式
- **动态比例尺**——底部显示当前缩放下"1厘米有多长"的动态刻度尺，随缩放自动切换合适的整数刻度（参考地图应用比例尺的实现逻辑）
- **主题线索跨卷筛选**——选中一个主题标签，全卷相关点位高亮并可跳转浏览
- **自动导览模式**——自动运镜+旁白讲解高潮场景，被动式体验，介于沉浸模式和学习模式之间，可复用方案B的相机运镜机制
- **场景环境音**——随卷轴区域变化的环境声，讲解时环境音duck
- **URL深链分享**——将当前位置+缩放级别编码进URL，无状态分享
- **"?"快捷键速查浮层之外的完整设计系统**——正式的视觉token（色彩、字体、间距、动效时序）尚未系统化整理，属于视觉设计阶段的工作，不阻塞MVP功能范围的确定

---

## 8. 待解决的实现细节（不是路线图之外的功能，是MVP范围内还没敲定的执行细节）

- 聚合的具体屏幕像素阈值数字——留到真实内容实测
- 分类筛选器（建筑/人物/物件/植物/动物）在当前"朱圈 + 分层显隐 + 悬停"模型下的具体交互，还没有专门过一遍细节
- 沉浸/学习模式切换控件的视觉形式：toggle开关 vs tab并列按钮，待视觉设计阶段决定
- 第一次进入学习模式时，是否需要一个引导提示告诉用户"把鼠标划过画面"这个悬停机制——因为整套交互不看提示的话不是很自明
- 悬停卡片的"缓冲延迟"方案（约250-300ms）是否足够，还是需要升级成"安全三角形"几何判定——留到真实原型实测再定
- 卡片"保护区"（判定卡片是否遮住了锚点本身所标注的画面元素）的具体算法和半径数字——用户明确要求"后面再聊"，暂不设计，方向大概率是每个锚点固定屏幕像素半径的keep-out区域，半径可能随tier变化
- 印章(seal)字符：目前只确认"建"对应建筑；人物/物件/植物/动物对应的字尚未最终拍板，等用户发来的设计图后再定稿
- **【已部分处理，基准已锁定】** 旧的182054×7595拼接结果宽高比≈24:1，与故宫官方画心比例528:24.8≈21.3:1对不上，说明含画心之外的部分。现已把卷首那幅金色装裱画切掉，新基准 **160348×7595 的宽高比≈21.1:1，已经非常接近官方21.3:1**——金色画正是当时"多余content"的主要来源。用户已决定**直接在这一版上标注**。之后又做了一次保守的上下裁边（去掉扫描白/黑背景边与底部校准色条，顶100/底60全分辨率px），最终坐标基准锁定为 **160348×7435**（详见 decisions-log "上下裁白边" 条目）；标注工具的TRUE_W/TRUE_H为160348/7435。
  - 图里仍保留了前隔水（卷首印章）和拖尾跋文/收藏印，所以还不是纯画心，但比例已足够接近，不再阻塞标注。
  - 若将来仍想裁到纯画心（去隔水/拖尾/比例尺条），会再次改变像素尺寸、需要给所有已标坐标做偏移换算——鉴于已开始标注，除非有强需求否则不做；真要做需评估坐标迁移成本。
  - 未来的动态比例尺功能：需先核对528cm是否精确对应当前图中画心的实际像素宽度（当前图含隔水，画心宽度≠整图宽度），这一步留到做比例尺时再处理。

---

## 9. 现有产出物

- `prototypes/indicator-prototype.html`（+ `assets/`）：验证marker+悬停引线机制成立，跨缩放锚点不漂移
- `prototypes/annotator-tool.html`（+ `prototypes/assets/`）：标注坐标采集工具，验证真实分辨率/工作副本分辨率换算机制成立，支持导出/导入JSON；已切换到新基准（TRUE 160348×7435，工作副本16000×742）
- **`清明上河图-完整拼接.jpg`（项目根目录）：官方拼接master、source of truth**——右→左拼接 + 已切金色画，64139×2974、约191MP（2.5×线性缩小；坐标基准是其代表的真实分辨率160348×7435，已切金色画+上下裁边）
- `image1-已裁金色画.jpg`：卷首金色画切除后的第一段，全分辨率21570×7595
- `assets/qingming-full-scroll-master.jpg`：【已作废】旧master，182054基准、含金色画，约86MP，仅历史参考
- `Full scroll/1.jpg`~`4.jpg`：原始扫描件（拼接前，宽度43276/50000/38778/50000，高均7595）
- Figma「清明」文件（`Final designs`页）：高保真组件与全屏mockup，包含分类印章、信息卡、聚合列表、模式切换、缩放控制簇等组件的实际视觉规格，**是当前颜色/字体等视觉数值的source of truth**，见§10

---

## 10. 视觉设计规范（颜色 / Design Tokens）

以下数值全部直接从Figma文件（`Final designs`页 → `Components`画板）里实际选中图层读出，不是目测估算。**Figma是颜色的source of truth**——以后视觉上有任何调整，以Figma里的实际数值为准，这份列表要跟着同步更新，不要反过来靠这份文档去"还原"Figma。

**已命名的颜色样式（Figma color styles）：**
| 样式名 | Hex | 用途 |
|---|---|---|
| 纸面 | `#FBF6EA` | 信息卡的纸张底色（卡片背景用80%透明度）。注：聚合标记已改为朱红实心圈，不再用纸面色 |
| UI金 | `#DCCCA8` | UI描边/边框类的金色 |
| 字黑 | `#1E2732` | 正文默认文字颜色 |
| 字金 | `#7F6019` | 金色文字（强调用途） |
| 字白 | `#FFFFFF` | 深色底上的文字颜色 |
| UI红 | `#B93A26` | 通用交互强调色——模式切换选中态、checkbox勾选态、信息卡正文里的行内链接，**统一使用这一个色，不随分类变化** |
| 胭虹 | `#7C4449` | 建筑分类的字印色；也是当前静止态朱圈 marker 的单色基准（视觉阶段可能再往朱砂方向推一点彩度，以 Figma 为准） |

**5个分类色（目前是直接写入的hex值，还没有像上面几个一样存成具名的Figma color style——建议视觉阶段收尾时补上，让分类色也进入统一的样式库，方便以后改色不用逐个图层找）：**
| 分类 | Hex | 说明 |
|---|---|---|
| 建筑 | `#7C4449`（胭虹） | 与上面"胭虹"样式同色 |
| 人物 | `#32788A` | 青蓝色 |
| 物件 | `#6B798E` | 灰蓝/石板色 |
| 植物 | `#6A8D52` | 灰绿色 |
| 动物 | `#BE8A2F` | 赭石/琥珀色 |

**其他实测数值：**
- 长卷背后的通用背景色（scroll容器兜底填充）：`#6F5438`
- 纸面色在不同场景下用的透明度：卡片/聚合标记背景 80%；另有一处 60%（具体用途待与视觉稿逐屏核对）
- 正文字体：Source Han Serif SC（思源宋体），Medium，14px，行高200%

**已确认但需要留意的点：**
- `UI红`（#B93A26）和`胭虹`（#7C4449）是两个不同的具名样式，不是同一个值——两者色相接近（都偏暗红），实际界面里放在一起时仍建议做一次并排对比，确认视觉上足够可辨识，不会被误认成同一个颜色
- 信息卡正文里的行内链接颜色固定用`UI红`，不随卡片所属分类变化——也就是说所有分类的卡片，链接颜色看起来都一样，是统一的"这是链接"的信号，不是"这是建筑类信息"的信号
- **静止态 marker 现在是刻意的单色朱红"朱笔圈点"，不做分类色变体**——早期那条"应给 marker 补齐5个分类色变体"的建议已作废；分类色只活在悬停卡片的字印上。marker 侧只需一套朱圈的三档大小（地标/场景/细节）+ 笔触/Multiply 质感规格
- **聚合标记现为朱红实心圈 + 汉字计数**（一…九，超过九显示"众"），单一固定尺寸；不再用纸面色（旧的"聚合默认中性色"决定已被推翻，理由见 §3.8）

---

## 11. 相关文档

- `CLAUDE.md` —— 现状速览，日常协作用的热缓存
- `decisions-log.md` —— 每个决策背后的推理过程、已放弃方案的详细原因
