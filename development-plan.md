# Development Plan — 清明上河图互动Web App

更新：2026-07-10 | 需求依据：`PRD.md` | 决策背景：`decisions-log.md` | 工程：`app/`（Vite + React + TS）

标注内容生产（用 `prototypes/annotator-tool.html` 打点）与工程开发并行进行，互不阻塞；两条线在 M3 汇合。

---

## Milestone 0 — 设计系统与组件库 ✅ 已完成

按 Figma「清明」`Final designs` 页实现基础组件，showcase 对照页验收（`/?showcase`）。

- [x] Design tokens（颜色/字体/圆角，`src/styles/tokens.css`）
- [x] 字体自托管：刻本宋（标题/印章）+ 思源宋体（正文，Noto Serif SC 兜底）
- [x] ModeToggle / IconButton / CategorySeal（印章 SVG 高保真）/ InfoCard / ListCard / NavBar / ScaleToast / AppBackground（渐变+莲花纹）

## Milestone 1 — 核心交互框架 ✅ 已完成

壳层交互与画布浏览体验，坐标系已按真实分辨率（160348×7435，含上下裁边）搭好，`src/viewer/`。

- [x] 学习/沉浸模式切换：沉浸时顶部 UI 上退、底部 UI 下退（定稿：滑出+渐隐，1000ms easeOutQuint）
- [x] 沉浸模式边缘热区唤醒（顶 120px / 底 160px，离开 200ms 缓冲），触发条件是"鼠标进入热区"而非任意移动（PRD §3.6）
- [x] 热区"武装"机制：切入沉浸后须待鼠标离开过热区一次才生效，避免 UI 退场后立刻被唤回；沉浸模式下 toast 照常出现（均 2026-07-10 定案）
- [x] 拖拽平移（5-8px 移动容差，偏向判定为拖拽）
- [x] 单击放大 ×1.5（在当前比例上 +50%），锚定点击像素，250ms 缓动（无双击档）
- [x] 滚轮缩放，锚定鼠标位置
- [x] 缩放范围：最小 = 整卷完整入画（fit 全览，动态）、暂定最大 = 扫描分辨率 500%【2026-07-10 改版，废弃"锁最小 100% 扫描"】
- [x] 比例基准 = **实物比例**（100% = 画高 24.8cm 按 CSS 96dpi 折算的真实物理大小 ≈ 扫描 12.3%），初始视图 = 卷首 + 实物 100%
- [x] 比例 toast：缩放时出现，约 1.2s 后自动消失（时长常量 `TOAST_HIDE_MS`，待实测调整）
- [x] 1:1 还原按钮：不常驻，只随 toast 一起出现（且比例 ≠100% 时）；点击回到实物 100%，保持当前观看位置中心不变
- [x] 底部导航条：实时视口指示框（位置+大小随缩放联动），点击跳转（350ms 运镜）
- [ ]（遗留）指示框视觉样式为占位方案，等视觉稿定稿后替换

## Milestone 2 — 深度缩放瓦片（画质架构）✅ 已完成（2026-07-10）

- [x] 瓦片切割脚本 `app/scripts/generate-tiles.sh`：libvips 虚拟拼接全分辨率原图（4→3→2→裁后1）→ 上下裁边（顶100/底60）→ 160348×7435 与坐标系精确一致 → DZI 金字塔（512px / overlap 1 / JPEG Q80，约368MB，已 gitignore）
- [ ] **⚠ 需重切瓦片**：现有瓦片是裁边前（7595）切的，脚本已加裁边步骤，需在本地重跑 `generate-tiles.sh`（本沙盒无 libvips 跑不了）
- [x] 按视口 + 缩放级别流式加载（`TileLayer.tsx`）：只挂载盖住视口的瓦片，离开视口即卸载；常驻工作副本低清打底，高清瓦片逐张淡入——低清先行、高清渐进
- [x] 加载进度指示：顶部 2px 细条 = "当前视口清晰度"（视口内目标层瓦片加载比例），满 100% 自动淡出；视觉为占位方案（本地太快看不到，生产网络下可见）
- [x] 最大缩放收到扫描 200%（2026-07-10 拍板，`MAX_ZOOM=2`，后续可调）；"放大到最大"按钮维持不加回
- [x] 性能：深倍缩放视口仅 4-20 张瓦片，按需 HTTP 加载 + 浏览器缓存
- [ ]（遗留 → M6）Vite build 会把 368MB 瓦片复制进 dist，部署时考虑瓦片独立托管或排除出构建
- [ ]（遗留 → backlog）全览时可见的段间接缝/色差来自扫描件本身曝光差异，属图像处理优化，不阻塞

## Milestone 3 — 标注系统 MVP（核心功能）🔨 2026-07-11 朱笔圈点改版落地

改版要点（对应 PRD §3.8 大改）：
- [x] 朱红手绘圈（Figma 标记 106:3698 三变体 SVG），单色朱红、mix-blend-multiply"进绢"（标注层移入画布内部保证混合生效，事件 stopPropagation 防冒泡触发画布缩放）
- [x] 三种圈按点位 id 稳定随机分配；无 idle 动画（取消呼吸）
- [x] 分层显隐：地标任何缩放可见 / 场景实物 40%→50% 淡入 / 细节 80%→100% 淡入（`TIER_REVEAL` 可调）；不可见点位不参与聚合
- [x] 聚合改版：实心朱圈 27px 固定尺寸 + 汉字计数（一…九，>9 显示"众"）；删除悬停列表，悬停仅光标变放大镜；单一点击路径 = 运镜放大至裂散
- [x]（首版，已被 route 1 取代）信息卡金色引用块用 `quote_zh/quote_en` 独立字段（Figma Citation 106:4206）
- [x] **route 1 已落地（2026-07-11）**：撤掉 `quote_zh/quote_en`，正文里以 `>` 开头的行 = 金色引用块（可穿插、多段），行内 `[文字](url)` 链接同套解析；`parseBlocks` 与标注工具同构（`InfoCard.tsx`）



- [x] 标注数据加载：`app/annotations.json`（用户持续打点直接改此文件，Vite 热更新即时生效）
- [x] marker 渲染：content 坐标锚点 + screen 固定像素尺寸，跨缩放不漂移（复用 viewer 变换矩阵）
- [x] marker 层级尺寸（地标 22 / 场景 16 / 细节 12px），呼吸 idle 动效
- [x] 悬停：marker 淡出、信息卡原地浮现（无引线）；marker 与卡片共享悬停态，280ms 离开缓冲
- [x] 卡片长内容内部滚动 + wheel/click 不透传画布；正文保留换行（pre-line）
- [x] 卡片正文按 Markdown 约定渲染（2026-07-11，见上方 route 1 项）
- [ ] **新增"印章"分类 → app 接线（PRD §3.8/§10，标注工具已支持）**：第 6 类，字印「印章」、色 `#8A2E1E`、不单独成档。app 待做：① Figma 导出印章类字印 SVG（字「印章」/色 `#8A2E1E`，同 65:320）；② `CategorySeal.tsx` `Category` 加 `'seal'` + `SEAL` 加条目；③ `annotations.ts` `TAG_TO_CATEGORY` 加 `印章:'seal'`；④ 分类色 token（若有）补 `#8A2E1E`
- [x] marker 点击 = 精确放大到锚点（×1.5 锚定数学复用）
- [x] 沉浸模式整层淡出（400ms，交互一并禁用）
- [x]（上方 2026-07-11 朱笔圈点改版已落地）单色朱红圈点 + 分层显隐——先前标注的"分类色圆点需返工"已完成，此处不再重复
- [ ] 卡片"保护区"摆放规则：目前是简化版（侧向偏移 24px + 屏幕边缘翻转），精确算法待与用户讨论（PRD §8）
- [ ] marker 相对锚点的固定偏移：调试期设为 (0,0) 便于校准坐标，正式值待定（`MARKER_OFFSET`）
- [ ] 学习模式首次进入的悬停机制引导提示（待用户拍板，见 ui-backlog #5）

## Milestone 4 — 密集区聚合 🔨 首版已随 M3 落地（2026-07-10）

- [x] 按屏幕像素距离贪心聚合（场景+细节参与，地标豁免），每帧重算 + 视口裁剪
- [x] 聚合改版已落地（见 M3 顶部 2026-07-11 项）：实心朱圈 + 汉字计数（一…九，＞九"众"）、删除悬停列表、单一点击路径、裂开全放；旧的三圆品字 / 中性色 / 列表 / 两条路径首版已被取代
- [ ] 双阈值防抖动：当前单阈值 48px（`CLUSTER_THRESHOLD_PX`），滞回机制拿真实标注数据实测后再加（PRD §8）

## Milestone 5 — 壳层完善（展开明细与已拍板决策见 `ui-backlog.md`）

- [ ] 双语切换：全局 toggle（默认中文），标注/UI 文案全量切换
- [ ] 背景音乐：开关（默认关闭）+ 音频资源
- [~] 键盘快捷键：`I` 三模式循环 ✅（2026-07-12，window 级监听、UI 淡出时依然生效、卷首页打开时不响应）/ `L` 语言 / `M` 音乐 / `?` 速查浮层——后三个未做
- [x] **三模式重构 + 自动展卷引擎（PRD §3.2 / decisions-log，2026-07-11）** ✅ 2026-07-12 落地并浏览器实测通过：
  - [x] **重命名**：学习→读画；旧"沉浸（UI 全淡出）"→**神游**（M1 的壳层淡出/边缘唤醒/热区武装机制原样复用给神游）；tab 文案缩为两字「读画/卧游/神游」（三 tab 在 1024px 下不挤，用户拍板）
  - [~] **auto-pan 引擎**（卧游/神游共用一台，`app/src/viewer/useAutoPan.ts`）：rAF + delta-time、速度乘子指数逼近缓停/缓起、**屏幕速度恒定**（放大后推进自然变慢）、方向恒向左；与 `animateTo` 帧级仲裁（增量/绝对写入者互斥，单击放大与分段跳转让路后无缝续推）。**⚠ 瓦片朝行进方向预取未做**（留待实测瓦片供给节奏后再定）
  - [x] **卧游**：auto-pan + 标记全在；**壳层与神游一样全隐、走同一套边缘热区唤出**（2026-07-12 用户拍板，取代首版"壳层常驻"——卧游与神游只差"标记显不显示"）；缩放不打断平移；悬停标记缓停/移开缓起（复用 280ms 悬停卡缓冲）；拖拽 scrub 后自动续播；进入从当前位置/缩放开始。**修了一个实测发现的坑**：缓停刹车期间锚点还会滑行 ~16px、会从静止光标下滑走导致卡片闪退——悬停态热区加倍（粘性滞回）解决
  - [x] **神游**：同引擎，marker + 壳层全隐，无悬停暂停；边缘热区唤出退出（含武装机制，现卧游/神游共用）
  - [x] **镜头持续原则**：切模式零镜头操作（无任何 animateTo/apply 调用），实测三模式互切镜头纹丝不动
  - [x] ~~**「重播」**~~ **已砍**（2026-07-12）：首版实现（淡切→瞬移回卷首+实物100%→缓起，底部比例簇旁）验证通过后，用户拍板去掉常驻按钮、不保留任何重播入口；卷尾停住后回看靠拖拽倒带（拖拽会清 ended latch）。相关代码（curtain 遮罩/ReplayIcon/REPLAY_FADE_MS）已一并移除，useAutoPan 的 `restart()` 接口保留备用
  - [x] 代码代号扩成三个：读画=learn / 卧游=travel / 神游=immerse（旧 immerse 语义最接近神游）；`MODE_ORDER` 供 tab 与 I 键共用
  - 可调参数（constants.ts，待实测调手感）：`AUTO_PAN_SPEED_PX_S=72`、`AUTO_PAN_STOP_TAU_MS=220`、`AUTO_PAN_RESUME_TAU_MS=380`、`REPLAY_FADE_MS=200`
  - 遗留观测项：卧游/神游下每帧 setView → App 树 60fps 常态重渲（MarkerLayer 投影+聚类逐帧重算），实测掉帧再考虑 transform 直写优化；min zoom 整卷入画时 tx 两端同为 0、一进卧游立即判卷尾（可接受边缘）
- [ ] 顶部按钮 hover tooltip（含快捷键提示）
- [ ] 分类筛选器（建筑/人物/物件/植物/动物，命中任一激活 tag 即显示）——交互细节需先过一遍（PRD §8）
- [~] **分段主题导览（PRD §3.11）** 🔨 首版已落地（2026-07-11）：顶栏正中 5 个纸签按钮常驻平铺、仅学习模式、点击运镜到 focal。数据 `app/segments.json` 已就绪（5 段、focal 全设、已校验）；采集工具 `prototypes/segment-tool.html`。**评审后待修（见 decisions-log "首版落地后的评审"）：**
  - [ ] **改字**："春娇乡野" → "春郊乡野"，且标题改为**从 `segments.json` 读取**（现为硬编码，两处不一致）
  - [ ] **当前段实时高亮**：视口中心 x 落在哪段就高亮哪段；激活态用 **UI红 `#B93A26`**（与模式切换统一），非激活=纸面+金描边
  - [ ] 确认：神游随壳层淡出；点击运镜落到该段 **focal**（不是段边缘）
  - [ ] 可选：段边界镜像到底部导览条，语义章节与空间导航对齐
  - [ ] 卡片金色引用块放**与正文不重复**的内容（原始文献/题记），别抄正文原句（PRD §3.8）
- [ ] 移动端检测提示"请使用更大屏幕观看"

## Milestone 6 — 打磨与发布

- [ ] 跨浏览器验证（Chrome / Safari / Firefox）
- [ ] 字体子集化（刻本宋 7.8MB → 按实际用字裁剪）
- [ ] 资源加载顺序与首屏体验优化
- [ ] 部署（静态托管）

---

## 参数待定表（拿真实内容/体验实测后定数）

| 参数 | 当前值 | 位置 |
|---|---|---|
| 比例 toast 停留时长 | 1200ms | `viewer/constants.ts` `TOAST_HIDE_MS` |
| 最大缩放倍数 | 500% | `MAX_ZOOM`（等瓦片最高精度层定） |
| 单击/拖拽判定容差 | 6px | `DRAG_THRESHOLD_PX` |
| 滚轮缩放灵敏度 | 0.0015 | `WHEEL_ZOOM_SENSITIVITY` |
| 沉浸热区高度 | 顶 120 / 底 160px | `EDGE_ZONE_*_PX` |
| 热区离开缓冲 | 200ms | `UI_LEAVE_DELAY_MS` |
| 聚合双阈值 | 未定（40-60px 起步） | M4 实测 |
| 卡片保护区半径/算法 | 未设计（用户定） | M3 讨论 |

## 非 MVP 停车场（PRD §7，不排期）

分段臂展格式（+切换 checkbox、`S` 键）/ 方案B next-prev 逐项浏览（自动展卷本体已由卧游/神游落地，剩手动逐项浏览）/ 动态比例尺（需先核对 528cm 对应画心像素宽）/ 主题线索跨卷筛选 / 自动导览的旁白层（展卷本体已落地，剩旁白/语音）/ 场景环境音 / URL 深链分享
